# Builds

Generated installers, packaged applications and local build artifacts belong here. Everything in this directory except this guide is ignored by Git.

- `native/` — future .NET/WinUI installers and packages.
- `legacy/` — NativePHP internal-test installers retained during migration.
- `generated-projects/` — temporary generated Laravel applications when explicitly exported here.
